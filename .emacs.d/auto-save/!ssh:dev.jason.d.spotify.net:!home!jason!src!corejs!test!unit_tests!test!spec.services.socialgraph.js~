/* Modules */
var common = require('../modules/common.js');
var expect = require('expect.js');
/** Include the different modules */
common.include('../../../src/js/namespaces.js');
common.include('../../../src/js/services.relations.js');

describe('SocialGraph', function() {

  var gateway, user, socialgraph;

  beforeEach(function() {
    socialgraph = new Spotify.Services.Relations();
    //relations.init(gateway, user);
  });

  describe('#getSubscribers', function() {
    it('Call success callback with current user\'s subset of subscribers', function() {
      expect(relations.currentUserRelations()).to.be(true);
      // expect the correct hermes call is made - verify params to hermes
      // expect the success callback is called with the correct data - verify data structure
    });
  });
});
